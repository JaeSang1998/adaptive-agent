{
  "name": "mock_news_fetcher",
  "description": "뉴스 헤드라인을 가져오는 도구. max_headlines로 개수 지정.",
  "input_schema": {
    "type": "object",
    "properties": {
      "max_headlines": {
        "type": "integer",
        "description": "가져올 뉴스 개수 (기본값: 5)"
      }
    },
    "required": []
  },
  "tags": ["news", "rss", "fetch"]
}
